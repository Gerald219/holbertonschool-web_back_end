# Python Async - Basic

Small introduction to async and await in Python.

Task 0 defines wait_random, an async coroutine that waits a random delay between 0 and max_delay seconds and returns that delay as a float.
