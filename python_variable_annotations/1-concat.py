#!/usr/bin/env python3
"""Module that defines a typed string concatenation helper."""

def concat(str1: str, str2: str) -> str:
    """Join two strings and return the combined text."""
    return str1 + str2
